{
    "name": "kukie/kukie-cookie-consent",
    "description": "Development tooling for the Kukie.io WordPress plugin. The plugin itself ships with no runtime dependencies - nothing here reaches wordpress.org.",
    "type": "wordpress-plugin",
    "license": "GPL-2.0-or-later",
    "require": {
        "php": ">=8.1"
    },
    "require-dev": {
        "phpunit/phpunit": "^10.5 || ^11.0 || ^12.0"
    },
    "scripts": {
        "test": "phpunit"
    },
    "config": {
        "sort-packages": true
    }
}
